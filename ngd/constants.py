"""Token-type IDs shared by the tokenizer (which lays out sequences) and the model
(which scatters embeddings into them). Values must match the official implementation."""

PAD_TOKEN = 0
IMG_TOKEN = 1
LANG_TOKEN = 2
PROPRIO_TOKEN = 3
ACT_TOKEN = 4
IMG_SEP_TOKEN = 5
GAME_ID_TOKEN = 6
