# Reference clones of the official repo (own .git; cloned on demand by ./env.sh test)
NitroGen/
third_party/

# Large artifacts (never push weights or data) -- root-anchored so ngd/data stays tracked
/checkpoints/
/data/
/outputs/
nitrogen.pdf
*.pt

# Python
__pycache__/
*.egg-info/
*.pyc
.venv/

# Misc
game_mapping.json
*.log
